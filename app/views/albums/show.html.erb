<div class="container">
  <br />
  <div class="row justify-content-center">
    <h2><%= @album.band + " \"" + @album.title + "\"" + " (" + @album.year.to_s + ")" %></h2>
  </div>
</div>
<div class="container">
  <table class="table table-hover">
    <thead class="thead-light">
      <tr>
        <th>Nr.</th>
        <th>Titel</th>
        <th>Dauer</th>
      </tr>
    </thead>
    <tbody>
      <% @album.songs.each do |a| %>
        <tr>
          <td><%= a.position %></td>
          <td><%= a.title %></td>
          <td><%= a.duration %></td>
        </tr>
      <% end %>
      <tr>
        <td colspan="2">Gesamtdauer</td>
        <td><%= @album.duration %></td>
      </tr>
    </tbody>
  </table>
  <%= link_to "Zurück", albums_path %>
</div>